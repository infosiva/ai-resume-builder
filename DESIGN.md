# DESIGN.md — ResumeVault

> **Auto-generated by design-pipeline.ts** — edit freely, re-run to regenerate.
> This file is the source of truth for all UI decisions in this project.
> When writing code, Claude reads this file and applies these conventions.

---

## Project Identity

| Field | Value |
|-------|-------|
| Product | ResumeVault |
| Tagline | AI resume builder that gets you hired |
| Category | CareerTech |
| Primary action | generate |
| Audience | student, professional, consumer |
| Mood | premium, serious, minimal |
| Competitor ref | like Zety but more AI-native |

---

## Design System: Apple

Inspired by **Apple**: Premium hardware feel, SF Pro, generous whitespace, product-showcase first

### Typography
SF Pro/system-ui, 17px body, massive 56-80px hero, tight tracking on display

**Brand fonts (install via next/font or Google Fonts):**
- Display/Hero: `font-black` with tight letter-spacing
- Body: `font-normal` 14-16px, 1.6 line-height
- Mono (numbers/code): `font-mono`

### Color Palette
Pure white or pure black, single blue #0071E3 CTA, gray hierarchy

**Brand colors:**
- Primary 1: `#0f172a`
- Primary 2: `#6366f1`
- Primary 3: `#22d3ee`

### Borders & Radius
12-18px cards, 980px max-width sections, very generous padding

### Shadows
None on dark, subtle on light: 0 2px 12px rgba(0,0,0,0.08)

### Spacing Scale
24/40/64/80/120px cinematic section spacing

### Design Principles
- One hero message per page — no clutter
- Product is the star — UI disappears
- Cinematic scroll reveals as hero
- Typography does heavy lifting

---

## Layout Archetype: Career Portfolio

Resume/career tools: professional, achievement-focused, credential-heavy.

### Section Order
1. **Nav with login** — `<NavAuth />` 
2. **Split hero (copy left, resume preview right)** — `<HeroResume />` 
3. **ATS score widget** — `<ATSScoreWidget />` 
4. **Template gallery** — `<TemplateGallery />` 
5. **Feature list** — `<FeatureList />` 
6. **Before/After comparison** — `<BeforeAfter />` *(optional)*
7. **CTA + Footer** — `<CTAFooter />` 

### Hero Style
`split` — Two-column: copy left, visual right

### Nav Style
`sticky-top` — Sticks to viewport top, always visible

### Card Style
`list`

---

## CSS Class Conventions

These global classes are pre-defined in `globals.css` — use them:

| Class | Use for |
|-------|---------|
| `glass-liquid` | Cards, containers, modals — frosted glass with iridescent border on hover |
| `liquid-blob` | Background ambient blobs (1/2/3 variants) |
| `text-iridescent` | Hero headline accent word or phrase |
| `pill-glass` | Badges, tags, small labels |
| `btn-liquid` | Primary CTA buttons |
| `reveal-3d` | Any card/section that should animate in on scroll |
| `badge-3d` | Floating numbered step badges, XP badges |
| `count-up` | Stat numbers — add `data-target`, `data-prefix`, `data-suffix` |
| `noise-overlay` | Film grain — add once at top of page |
| `depth-grid` | Perspective grid for technical/finance hero sections |

---

## Key Features to Showcase
- ATS-optimized resumes
- Job description matching
- One-click formatting
- Export to PDF/Word

---

## Design Research Sources

### Google Stitch Prompt
Paste this at **labs.google/stitch** to generate a UI layout reference:

```
AI resume builder landing page, professional and premium. Dark navy background. Split hero: left = bold headline "Get hired 3x faster" + subtext + CTA button; right = animated resume document preview with AI highlighting sections. Below: ATS score indicator (circular gauge). Template gallery: horizontal scroll of resume templates.
Color palette: #0f172a, #6366f1, #22d3ee. Brand mood: premium, serious, minimal.
Target audience: student, professional, consumer.
```

### Pinterest Search Queries
Search these on Pinterest to build a moodboard:

1. `careertech app UI design premium 2024`
2. `split hero section web design dark`
3. `resumevault landing page design inspiration`
4. `careertech SaaS website design 0f172a`
5. `premium medium density UI kit`
6. `web design Zety but more AI-native inspired`

### getdesign.md Reference
Design system: **Apple** at `getdesign.md/design/apple`

---

## Claude Implementation Notes

Resume preview = styled div with realistic resume layout. ATS gauge = SVG circle progress. Template cards scroll horizontally on mobile. Professional color = deep navy + indigo + cyan accent.

**When Claude writes code for this project:**
1. Check this DESIGN.md before any UI decision
2. Use brand colors `#0f172a, #6366f1, #22d3ee` — no other primary colors
3. Follow Apple spacing/radius conventions above
4. Apply layout archetype section order strictly
5. Every card → `glass-liquid`, every badge → `pill-glass`, hero text → `text-iridescent`
6. Nav style: `sticky-top` — see layout archetype

---

*Generated: 2026-05-07 by design-pipeline.ts*
